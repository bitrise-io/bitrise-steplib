title: generate-diff-image
summary: |
  Generate a difference image from two images.
description: |
  Generate a difference image from two images. Images can be specified by file or directory. Useful for checking UI etc.
website: https://github.com/kvvzr/bitrise-step-generate-diff-image
source_code_url: https://github.com/kvvzr/bitrise-step-generate-diff-image
support_url: https://github.com/kvvzr/bitrise-step-generate-diff-image/issues
published_at: 2019-07-24T23:30:30.886377+09:00
source:
  git: https://github.com/kvvzr/bitrise-step-generate-diff-image.git
  commit: 53b1077e73c97e49fdabbab5d1577baf922a1a88
host_os_tags:
- osx-10.10
- ubuntu-16.04
type_tags:
- utility
toolkit:
  go:
    package_name: github.com/kvvzr/bitrise-step-generate-diff-image
deps:
  brew:
  - name: git
  - name: wget
  apt_get:
  - name: git
  - name: wget
is_requires_admin_user: true
is_always_run: false
is_skippable: false
run_if: ""
inputs:
- before_images: null
  opts:
    description: |
      Input the file path of the image you want to compare.
      `Before images path` and `After images path` must both be directories or files.
    is_required: true
    summary: The file name or directory of before images.
    title: Before images path
- after_images: null
  opts:
    description: |
      Input the file path of the image you want to compare.
      `Before images path` and `After images path` must both be directories or files.
    is_required: true
    summary: The file name or directory of after images.
    title: After images path
outputs:
- GENERATED_DIFF_IMAGES_DIR: null
  opts:
    description: |
      Directory of generated difference images. Note that no image is generated if `before image` and `after image` are the same.
    summary: Directory of generated difference images.
    title: Directory of generated difference images
